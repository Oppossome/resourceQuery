{
	"type": "module",
	"version": "0.0.0",
	"name": "@resourcequery/core",
	"packageManager": "yarn@3.4.1",
	"main": "./src/index.ts",
	"scripts": {
		"test": "yarn test:types --no-watch && yarn test:unit",
		"test:unit": "yarn node --expose-gc \"$(yarn bin vitest)\"",
		"test:types": "vitest --typecheck",
		"coverage": "vitest run --coverage"
	},
	"dependencies": {
		"ts-toolbelt": "^9.6.0",
		"zod": "^3.22.4"
	},
	"devDependencies": {
		"typescript": "^5.3",
		"vitest": "1.0.0-beta.6"
	}
}
